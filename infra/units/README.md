# units

Terragrunt unit templates — referenced by `source` in each stack’s `terragrunt.stack.hcl`. Terragrunt generates runnable units under `<stack>/.terragrunt-stack/`.

| Unit | Module |
|------|--------|
| `lambda-code-bucket` | [`modules/lambda-code-bucket`](https://github.com/rx-refill-reminders/terraform-modules/tree/main/modules/lambda-code-bucket) |
